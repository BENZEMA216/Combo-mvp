# 项目级智能体协作约定

- 开始工作前先读取根目录 `CLAUDE.md`、本文件，以及任务涉及目录中的说明文件。
- 涉及部署、命名空间、workflow、基础资源、域名或发布流程的改动，必须先阅读 `docs/deployment-topology.md`（部署拓扑与规范，仓库内部署系统的权威说明），并确保改动与之一致；不一致时应先更新该文档再改代码，部署相关 PR 必须对齐它。
- 任何会修改受 Git 跟踪文件的功能、修复、重构、测试、文档、维护或持续集成贡献，都必须调用 `$github-collaboration`。只读检查、解释和纯规划不需要创建工作树。
- 默认分支所在的主检出目录只作为控制面使用，保持没有受跟踪文件的改动；不要直接在这里开发、提交或合并。
- 一个任务对应一个负责人、一个工作树、一个分支和一个拉取请求。同一工作树不得由多个并发智能体共享，也不得把同一分支同时签出到多个工作树。
- 新工作树必须基于用户指定的规范上游远端基线；用户未指定时，外部派生仓库贡献者使用最新的 `upstream` 默认分支，有写权限的协作者使用最新的 `origin` 默认分支，不得以可能过期的本地默认分支为基线。
- 每个任务必须记录创建工作树时的不可变基准 SHA、当前 `HEAD` SHA 和 dirty 状态。需求真源、代码、运行环境或验收证据互相冲突时，先停止并报告，不得自行选择一个版本继续开发。
- 合并前必须完成与改动范围对应的本地测试、仓库完整质量门禁、差异自检、拉取请求复核以及 GitHub 必需检查。失败、未运行或仍有未解决评审讨论时不得合并。
- 严格区分已开发、已验证、已提交、已推送、持续集成通过、已合并和已部署，每个结论都必须绑定对应提交 SHA 和可复核证据。URL 本身不是版本证据，Mock 或 Mixed 体验不得描述为完整真实链路。
- 推送、创建或修改拉取请求、请求评审者、合并、删除远端分支都是外部写操作，只在用户请求已涵盖或用户明确授权时执行。无论权限如何，都不得绕过分支保护。
- 只有确认拉取请求已合并、工作树干净且没有未推送工作后，才能按技能的清理流程移除工作树；不得使用强制删除掩盖未保存的工作。
- 本协作流程不授权 Test、Preview 或 Production 部署，也不授权任何 Kubernetes、主机路由或数据操作。

## 代码评审规则

- 检查功能正确性、边界条件、错误处理、兼容性、权限与敏感数据风险，以及测试是否覆盖了改变的行为。
- 将阻塞合并的问题标为必须修复；纯偏好建议明确标为非阻塞，避免把个人风格当成质量缺陷。
- 智能体自检不能替代独立复核。提交者不得把自己的复核视为审批；重要或高风险改动需要维护者或另一名获授权的评审者复核。
