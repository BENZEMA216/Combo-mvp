# Snapshot 测试脚本说明

`run-minio-e2.mjs` 使用固定镜像摘要启动一次性 MinIO 容器，给它分配随机的本机回环端口、临时管理员凭据和内存文件系统，再显式运行对象存储组件集成测试。脚本无论测试成功或失败都会删除该容器和临时凭据文件。

这个脚本只建立测试用对象存储环境，不配置生产 IAM、Object Lock、KMS、备份或灾难恢复。默认单元测试不会隐式启动 Docker；需要真实 MinIO 证据时使用包内的 `test:minio:e2` 命令。
