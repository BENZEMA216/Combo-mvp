# Creator Agent Protocol

这个包是 Creator-hosted Agent VNext 的跨进程契约真源。云端服务、Creator Worker、Sandbox Supervisor、Creator Console、消费者页面和验收工具应从同一套运行时 Schema、TypeScript 类型与生成产物读取协议，不能在各自应用里复制另一份字段定义。

## 目录职责

- `src/` 定义共享协议（包括 Snapshot Manifest、archive/manifest 两类加密 Envelope、AgentVersion、Broker、Worker Conversation Ready/Invocation durable facts、Host turn terminal、Sandbox 与 HTTP）、RFC 8785 JSON 规范化、Invocation 状态机、稳定错误和重试策略、Evidence Bundle、不变量、测试注册表，以及 AgentVersion、闭世界 Context Tools、Execution Capability 上游计数、Snapshot Manifest 总量、单文件、压缩对象、压缩比和相对路径 UTF-8 bytes 的独立边界证据目录。
- `fixtures/` 保存 Schema、WSS、状态机、canonical digest、AgentVersion及其digest语义矩阵、Client Idempotency UUIDv4、闭世界 Context Tools、Execution Capability 上游计数、Snapshot Manifest 总量、单文件、压缩对象数值、真实 canonical 50 MiB ±1 压缩 recipe、压缩比与相对路径 UTF-8 bytes 资源边界和 Evidence Bundle 的 golden vectors；`index.json` 绑定每个 fixture 的 SHA-256。
- `schemas/contract-schemas.v1.json` 是从运行时 Zod 真源生成的 JSON Schema bundle。
- `schemas/broker-contract.v1.json` 独立冻结 Broker handshake、registration、wire Envelope、Conversation Open authority、逻辑命令摘要、JCS canonicalization、WSS connect path/frame 上限与完整 close code/reason map；注册与握手携带它的 RFC 8785 SHA-256，但该产物自身不写入 digest 值。
- `openapi/creator-agent-v1.openapi.json` 是 Creator 与 Consumer HTTP API 的 OpenAPI 3.1 契约。
- `scripts/` 负责生成、校验契约产物，以及用固定 base seed和100个唯一派生 seed运行 Invocation、Conversation Ready、Conversation Open 与 Execution Capability 属性测试；每个 model 的总运行数保持100,000。

## 协议边界

包内冻结 `AgentVersionManifest/1`、`SnapshotManifest/1`、archive/manifest 两类加密 Envelope、Snapshot publication preparation/commit marker、`combo.creator-broker/1`、Invocation 状态机、`SandboxSpec/1`、`SandboxAttestation/1` 和 Creator/Consumer HTTP API。两类 Envelope 绑定最终对象 key、摘要、明文长度、KEK key id 和各自固定 binary framing，并要求同一 Snapshot 的 Creator、digest、wrapped DEK 一致且 nonce 不同。Snapshot upload API 只在 Worker 已完成两个密文对象后创建会话，并一次返回两个绑定 exact length、canonical base64 checksum、`If-None-Match` 和完整 metadata 的 Signed PUT。preparation marker 以 bounded canonical JCS 冻结首个已验证密文对，commit marker 只 hash-link exact preparation bytes并作为唯一读可见性权威；两者均使用 Creator/Snapshot 派生 fixed key 和 strict unknown-key contract。Schema 采用严格 unknown-key 策略；Broker JSON frame 还在运行时拒绝重复 JSON key和超限 frame。Fence 与 sequence 在 wire 上统一使用 canonical uint63 十进制字符串，避免 JavaScript number 精度损失。

Snapshot Manifest 的 4 MiB 只作为加密对象/read-all raw defense ceiling，不是可达语义 maximum。`SnapshotManifestSchema` 在 `mediaType<=128`、2,000 files、每路径512 UTF-8 bytes、单文件10 MiB和总展开200 MiB的共同约束下，可达 canonical 全局上界为2,536,575 bytes；Snapshot production parser 以该较小值在 JSON parse 前拒绝 N+1。两侧共享 Protocol 常量，不能用 whitespace、unknown key 或无界 media type填满 raw defense cap。

Worker registration capability 与 Broker handshake 复用同一份 strict schema。两者都必须携带单个 `brokerContractDigest`；该字段属于 unsigned handshake 的固定字段并进入 DeviceSigner 的 canonical signing bytes。接收方必须将 registration、handshake 与 `currentBrokerContractDigest()` 精确比较，缺失、未知字段或任一 digest 漂移都应 fail closed。

`protocol-compatibility.v1.json` 以两个 exact Worker profile、Gateway N-1/N 和四个显式 pair 冻结 G0 兼容矩阵。每个 Worker profile 绑定独立 handshake golden、Worker version、Codex artifact/schema 与 isolation mode；各维值即使分别被接受，只要索引 profile 未声明也必须阻断。该 corpus 和 matcher 只证明注册/握手兼容决策，不证明 Runtime、Snapshot、Host、公开 WSS 或 Deployment readiness。

`requestDigest`、`contentDigest` 和 `resultDigest` 使用按租户或版本密钥计算的 domain-separated HMAC-SHA-256；Snapshot 与公开构建产物继续使用普通 SHA-256 内容寻址。这些 digest 不代替 AEAD、签名、Lease 或 Execution Capability。

Creator/Consumer OpenAPI 中 11 个由服务端分配 ID 的路径参数统一引用 `ServerId`，其运行时与公开 Schema 都只接受 36 字符小写 UUIDv7。ADR-VNEXT-033 将 9 个公开 `Idempotency-Key` header 与 send/retry `clientMessageId` 冻结为同一 36 字符 canonical lowercase UUIDv4 约束；header 统一引用 `IdempotencyKey` component，body alias 发布等价 inline schema 与 binding extension，send/retry 还必须逐字绑定两者。Client key 不用于排序、授权或模型输入，fresh admission 不接受 legacy key，已有 bounded legacy 行只允许在 durable replay 命中后回读。Evidence reviewer signoff 和不变量注册表的 Gate 集合最多包含 9 个且不得重复；reviewer signoff 继续要求词法递增，不变量注册表继续保留仅唯一、不强制排序的既有语义。

闭世界 Context Tools corpus 直接引用技术方案 7.8 与 ADR-VNEXT-013/031，以 exact、empty、missing、reordered、duplicate、unknown replacement 和 unknown extra 七个 digest-bound 变体覆盖四个运行时 parser、四个公开 contract/OpenAPI owner 和本地 `buildAgentVersion` 共 63 个结果；它只证明三工具 tuple 不能删减、重排、重复或扩张，不证明 G2 isolated runtime、E4 real Linux Codex 或 E5 real Mac isolation。`SCH-004/009/010` 均已由各自 closure/profile 证据实现，但正式 T0 Linux 与更高 Gate 仍须单独运行。

Broker whole-wire corpus 把 ADR-VNEXT-034 冻结的 65,536-byte frame product policy 与 runtime 及当前 `broker-contract.v1.json#/maxFrameBytes` 对齐：两个 Protocol parser 执行 6 个 N-1/N/N+1 结果，真实回环 Gateway handshake/established 与 Worker first-lease/established 再执行 12 个结果。N+1 target payload 不进入 Gateway authenticate/accept/replay/gap 或 Worker activate/commitInbound/replay/gap；transport 关闭仍可触发正常的 `closeSession`、`releaseConnection`、重连和新 Lease lifecycle。证据不覆盖 TLS/public WSS、fragmentation/extension negotiation、load/backpressure、E3 公网云或 Production，且不完成 `SCH-004`。

Raw-ingress hostile corpus 对 12 个直接公开 JSON ingress owner 执行同一套 76 probes：Broker handshake/frame、Snapshot preparation/commit marker、Evidence 七个 structured byte owner 与 Snapshot Manifest，共 912 个结果。五类 malformed UTF-8、escaped lone surrogate、除 TAB/LF/CR 外的 62 个 C0/C1 与 BOM/syntax/duplicate/unknown 输入都必须得到稳定低基数错误且不回显 canary；每次调用只证明输入 bytes 未被修改，不把纯 parser 性质冒充 Gateway/Worker/S3/PG business zero-mutation。9 个 HTTP request root 与 Runtime raw JSON error 由 dedicated SCH-005 closure/Runtime 测试补齐；Content-Encoding/chunked、正式 T0 Linux evidence 和 `SNP-010` 的完整 Snapshot Gate 仍单独保留。

资源边界 corpus 只证明运行时 parser 与公开 contract/OpenAPI 在已声明数值上的一致性，不替代产品政策冻结。Broker capacity corpus 以 0/1/2 冻结握手中的两个 singleton 容量字段，并覆盖 Protocol、真实 Gateway 回环 WebSocket/WS 与真实 Worker 出站握手；它不证明 TLS/WSS termination、public ingress、Creator/Conversation WIP、queue、PostgreSQL/SQLite admission、真实推理容量或 soak。Execution Capability 上游计数 corpus 直接引用 P0 `INV-010`、ADR-VNEXT-010 和测试方案的 at-most-once/Provider sink 断言，以同一组 `UNUSED=0`、`DISPATCHED=1`、非法 `DISPATCHED=2` 记录执行 Schema、决策函数、公开 contract 和真实文件 SQLite 共 12 个结果；它只证明数值边界与 SQLite `CHECK`/重启读取，不证明真实 Provider sink、并发抢占、crash/network fault、10k 随机序列、E4 或 E6，也不完成 `INV-010`。Snapshot Manifest 的文件总数 2,000 与展开后 200 MiB 直接引用技术方案 5.2 和测试方案 SNP-002/003/006/007；独立的单文件 corpus 引用同一技术方案、SNP-004/005 和既有 ADR-VNEXT-003，并由 Snapshot 包使用真实非稀疏 UTF-8 文件补充 build-to-verify 机制证据。原压缩对象 corpus 仍只冻结 50 MiB metadata、36-byte AEAD framing 和公开数值 owner；新增的独立 production-mechanism fixture 则以 compact SHAKE256/base32 recipe 绑定真实 52,428,799/800/801-byte 单 frame tar.zst、逐文件/Manifest/tar/archive digest 及 builder/verifier 结果。它关闭仓库内 `SNP-008` 实现缺口，但本机结果不冒充尚未运行的 Linux SHA-bound G1 evidence、MinIO/PostgreSQL E2、AEAD 发布链或云端 VERIFIED 状态。压缩比 corpus 引用技术方案 5.2、测试方案 8.3 与 ADR-VNEXT-003，以 digest-bound 99/100/101 整数 probes 对齐决策与运行时 helper；Snapshot 包另行生成 1 MiB counter-hex accepted archive 和 1 MiB ASCII `a` canonical bomb，并在 fake S3 发布流程证明 bomb 不产生 preparation、final 或 commit。相对路径 corpus 引用技术方案 5.2、SCH-004/005、SNP-009/010 和 ADR-VNEXT-004，以 ASCII/CJK/astral 混合路径冻结 512 UTF-8 bytes，并以全部 65 个 C0/C1 和双 lone surrogate 驱动严格路径 owner；文件系统 component 255-byte 政策尚未冻结。UTF-8 byte owner现在按字段语义拆分：8 个多行正文 owner只允许 TAB/LF/CR，11 个 name/version/model/adapter/worker/errorCode结构 owner拒绝全部C0/C1；二者继续使用 ASCII、CJK 和 emoji 验证 N-1/N/N+1 精确byte上限，所有超限错误必须命中目标issue且不回显canary。Unicode code-point owner同样分为15个多行正文与14个strict path/token/error owner，47个公开节点中的29个使用strict pattern；exact-derived object key仍由运行时精确派生约束补充结构Schema。Dedicated `SCH-005` closure从运行时真源现场生成contract/OpenAPI/Broker文档，拒绝任何无pattern/enum/const或未知开放pattern的string node，并以固定无input/issues/cause的boundary覆盖9个HTTP request root；未实现route只构成schema证据，不声称durable副作用。Signed PUT URL继续在WHATWG解析前使用strict scalar/no-control，且本证据不新增URL maxLength或一般URL canonicalization语义。Compatibility、decoded/UTF-8/structural/resource corpus的plain metadata owner继续使用strict scalar/no-control schema；duplicate-key错误仍只保留稳定reason与offset。Broker handshake/frame与Snapshot marker公开raw parser统一抛出固定`ProtocolRawInputError`。ADR-VNEXT-033与独立Idempotency corpus继续冻结9个header和send/retry alias的canonical UUIDv4；未来send/retry handler仍需真实zero-mutation证据。ADR-VNEXT-034已把1882个runtime source rows、750个generated artifact rows、233个source AST rows、15个finite string family、14个delegated fixtures与7个manual caps逐项绑定到actual probe，implementation remaining为零；Snapshot canonical semantic maximum为2536575 bytes，4 MiB仅为独立encrypted/raw defense cap。`SCH-004/005`均已绑定仓库测试，`SNP-008`也已绑定本机 production mechanism；正式 T0/G1 Linux PASS 仍需 clean SHA-bound workflow evidence，`SNP-009`继续保持`planned`。

Worker 的 `prepared/started/succeeded/failed/cancelled/uncertain` 事实使用 `combo.worker-invocation-fact/1`。事实 ID 不是调用方任选值：prepared 固定使用 `prepareCommandId`，started 固定使用 `startCommandId`，全部 terminal fact 固定使用 `invocationId`；它不能复用会随 connection 重封装的 WebSocket `messageId`。事实内的 Lease/Fence 表示最初执行权威，重连后可以由新的外层传输 Lease/Fence 承载同一事实，但事实本身和 `factDigest` 不得改变。started 必须保存可查询的 Host `runtimeThreadId/runtimeTurnId`，succeeded 必须重复这两个 handle 并绑定 `startedFactDigest`。cancelled 必须携带下面的 exact interrupt receipt，且 receipt digest 与 Invocation、Version、Snapshot、Execution Capability、原 Lease/Fence 全部逐项绑定。`factDigest` 对 Version、Snapshot、Execution Capability、原执行 Lease/Fence 及事件专属非敏感字段做 canonical SHA-256。Worker SQLite 与 PostgreSQL 必须分别重算并保存同一 digest，同 ID 不同 digest 只能 security-block，不能猜测或覆盖。

Worker interrupt receipt 使用严格 `combo.worker-interrupt-receipt/1`。`PROVED_NOT_EXECUTED/LOCAL_DISPATCH_COUNTER` 只允许两种零 dispatch lineage：PREPARED 时 `startCommandId/dispatchNonce` 同为 `null`，或 STARTING intent 已 durable 时两者同为 UUID；两者的 runtime/dispatch receipt/sandbox/Host terminal 证据全部为 `null`。`INTERRUPTED/HOST` 要求 dispatch count 为1，start/dispatch nonce、runtime thread/turn、dispatch receipt、sandbox instance/attestation 与 Host terminal digest 全部存在。receipt 只允许控制 ID、原因、计数和 digest，不允许 Prompt、答案、raw Host JSON、路径、token 或凭据。当前仅冻结 Protocol/Broker schema、canonical digest、golden 和 mutation contract；Worker producer/SQLite 原子性、Host interrupt 真验证、Cloud/Gateway cancelled projection、真实 Codex 不二次 dispatch、`INV-010/016` 与相应 higher Gate 仍未由本包完成。

Host turn terminal 使用严格 `combo.codex-app-server-turn-terminal/1`。它把 exact thread/turn 与规范化 `turn/completed` observation 冻结为 `SUCCEEDED`、`FAILED(TURN_FAILED|TURN_TIMEOUT)` 或 `CANCELLED`，只公开 outcome、稳定错误码与 SHA-256 digest；Prompt、答案、路径、raw Host JSON 和诊断文本都不进入 evidence。缺失 terminal、Host loss 或 binding mismatch 必须由 Worker 收敛为 `UNCERTAIN`，不能从一般 Error 或空 RPC 响应推断稳定终态。该协议只冻结本机 Host evidence contract，不证明跨进程 query/reattach、Isolation attestation 或 Cloud projection。

Worker 的 `conversation.ready` 事实使用独立且严格的 `combo.worker-conversation-ready-fact/1`。`sourceEventId` 固定等于产生该事实的 durable `openCommandId`；fact 同时冻结 Conversation、Deployment、AgentVersion ID/digest、Snapshot digest、原 Worker installation/session/Lease/Fence、Sandbox、Host runtime thread 与 ready evidence digest。Broker body 必须是这些 exact fact 字段加 canonical `factDigest`，不能加入 transport receipt 等旁路字段。外层 `messageId` 必须与 source 不同，`correlationId` 固定等于 `conversationId`；授权重连可以更换外层 connection/message/sequence/session/Lease/Fence，但不能改写原 fact 或强制原 Lease 等于当前外层 Lease。

`conversation.open` body 必须携带 `openAuthority`，冻结原 Deployment、Installation、Worker Session、Lease 与 Fence。Open command 的 `correlationId` 固定等于 Conversation ID，`messageId` 与 Conversation ID 必须不同，当前外层 Deployment 必须等于原 Deployment；授权重封装只能改变 connection、sequence、时间和当前 Session/Lease/Fence。`brokerConversationOpenLogicalDigest()` 只 hash protocol、schema、kind、type、messageId、correlationId 与完整 body，不把这些外层传输字段混入稳定业务身份。

## 验证

```bash
pnpm -F @cb/creator-agent-protocol generate:contracts
pnpm -F @cb/creator-agent-protocol check:contracts
pnpm -F @cb/creator-agent-protocol typecheck
pnpm -F @cb/creator-agent-protocol typecheck:test
pnpm -F @cb/creator-agent-protocol test:g0
pnpm -F @cb/creator-agent-protocol test:property --seed 12648430 --seeds 100 --runs 100000
pnpm vnext:test:g0
```

`generate:contracts` 只在有意修改运行时真源或 fixture 时使用；持续集成运行 `check:contracts`，任何生成结果或 fixture digest 漂移都会失败。根级 `pnpm vnext:test:g0` 是正式 G0 的唯一命令：除本包外还执行 Snapshot manifest/path、Gateway compatibility、Runtime public ingress登记测试及T0 workflow/evidence防漂移contract，并把需要真实 PostgreSQL 的两项 SCH-010 supporting test明确留在T1。属性测试在失败时必须保留 base seed与派生 seed；它证明状态机逻辑，不证明真实数据库、Broker、Codex 或 Sandbox。Reusable Linux workflow与evidence生成器已经实现，但当前 dirty工作区尚无clean commit或远端run，因此正式T0状态仍是`NOT_RUN`。
