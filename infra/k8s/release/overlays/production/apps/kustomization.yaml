apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: combo-prod
resources:
  - ../../../base/apps
labels:
  - includeSelectors: true
    pairs:
      combo.build/release-track: v1
