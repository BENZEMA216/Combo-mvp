apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: combo-test
resources:
  - ../../../base/apps
patches:
  - path: runtime-visible-transcript-test-keyring.patch.yaml
labels:
  - includeSelectors: true
    pairs:
      combo.build/release-track: v1
