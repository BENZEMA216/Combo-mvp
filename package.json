{
  "name": "@cb/root",
  "private": true,
  "version": "0.0.0",
  "description": "创作者中心主链路 monorepo（脊柱 + shared 包 + apps 骨架）",
  "type": "module",
  "engines": {
    "node": ">=24",
    "pnpm": ">=11"
  },
  "packageManager": "pnpm@11.0.9",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/**\" --filter \"./apps/**\" run build",
    "build:shared": "pnpm -F @cb/shared run build",
    "typecheck": "pnpm -r run typecheck",
    "typecheck:test": "pnpm -r --if-present run typecheck:test && tsc -p tsconfig.e2e.json",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test:workflow-contracts": "node --test scripts/verify-preview-release.test.mjs scripts/environment-boundary-contract.test.mjs scripts/source-integrity.test.mjs scripts/vnext-g0-suite.test.mjs scripts/vnext-gate-honesty.test.mjs scripts/vnext-golden-decision-table.test.mjs scripts/vnext-t0-evidence.test.mjs scripts/vnext-t0-workflow-contract.test.mjs scripts/vnext-nginx-routing.test.mjs && python3 .agents/skills/github-collaboration/scripts/test_worktree_guard.py",
    "vnext:test:g0": "node scripts/run-vnext-g0.mjs",
    "vnext:test:fast": "pnpm vnext:test:g0",
    "vnext:test:property": "pnpm -F @cb/creator-agent-protocol test:property",
    "test:fast": "pnpm -r --workspace-concurrency=1 --filter '!@cb/scripts' run test && pnpm test:workflow-contracts",
    "test:local": "pnpm -r --workspace-concurrency=1 --filter '!@cb/scripts' run test && pnpm test:workflow-contracts",
    "test": "pnpm -r --workspace-concurrency=1 run test && node --test scripts/verify-preview-release.test.mjs scripts/environment-boundary-contract.test.mjs && python3 .agents/skills/github-collaboration/scripts/test_worktree_guard.py",
    "dev:preflight": "bash scripts/dev-preflight.sh",
    "release:verify:preview": "node scripts/verify-preview-release.mjs",
    "e2e:resend-auth": "bash scripts/integration/resend-auth-e2e.sh",
    "dev": "pnpm -r --parallel run dev",
    "clean": "pnpm -r exec rm -rf dist tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "@playwright/test": "1.62.0",
    "@types/node": "^22.10.5",
    "eslint": "^9.18.0",
    "eslint-config-prettier": "^9.1.0",
    "fast-xml-parser": "5.7.3",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.20.0",
    "vitest": "^2.1.8"
  }
}
